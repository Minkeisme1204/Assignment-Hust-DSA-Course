;

    // char *display = malloc(sizeof(char) * (size - 1));
    // char *code[256];
    // for (int i = 0; i < 256; i++) code[i] = malloc(sizeof(char) * (size - 1));
    // printCodes(fopen("Code.txt", "w"), root, display, 0, code);