hile (list->size > 0) {
    //     Node *pCurrent = list->head;
    //     for (int i = 0; i < m; i++) {
    //         pCurrent = pCurrent->next;
    //     }
    //     Node *pNext = pCurrent; 
    //     deleteNode(pCurrent, list);
    //     pCurrent = pNext->next;
    //     list->size--;
    // }